# Lightweight dependencies for CI/CD testing
# Excludes heavy ML libraries (tensorflow, keras, ultralytics) that aren't needed for unit tests
# Modules use lazy imports with try/except to handle missing optional dependencies

# Web framework
fastapi>=0.109.0
uvicorn[standard]>=0.27.0
python-multipart>=0.0.9
httpx>=0.26.0

# Core data processing
pandas>=2.1.0
numpy>=1.26.0
scipy>=1.12.0
pillow>=10.2.0

# Machine learning (lightweight)
scikit-learn>=1.4.0
xgboost>=2.0.0
shap>=0.45.0
joblib>=1.3.0

# Computer vision (headless for CI)
opencv-python-headless>=4.9.0

# Database
sqlalchemy>=2.0.0
alembic>=1.13.0

# Data validation
pydantic>=2.5.0
pydantic-settings>=2.1.0

# Visualization
plotly>=5.18.0
matplotlib>=3.8.0
seaborn>=0.13.0

# Utilities
python-dotenv>=1.0.0

# Note: The following heavy dependencies are EXCLUDED for CI:
# - tensorflow>=2.15.0 (not used in current codebase)
# - keras>=2.15.0 (not used in current codebase)
# - ultralytics>=8.1.0 (not used in current codebase)
# - streamlit>=1.31.0 (only needed for dashboard UI, not for API tests)
